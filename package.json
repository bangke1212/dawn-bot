{
  "name": "dawn-bot",
  "version": "2.0.0",
  "description": "DAWN Bot — Python backend + React frontend",
  "scripts": {
    "dev": "concurrently \"cd server-py && python3 run.py\" \"cd client && npm run dev\"",
    "start": "cd server-py && python3 run.py",
    "install:all": "npm install && cd client && npm install && cd ../server-py && pip install -r requirements.txt",
    "build": "cd client && npm run build"
  },
  "dependencies": { "concurrently": "^9.0.1" }
}
